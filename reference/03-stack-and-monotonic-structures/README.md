# Stacks and monotonic structures

A stack or deque kept in sorted order, so an arriving element resolves every element it dominates at once. Each index enters and leaves once, which is what makes an apparently quadratic loop linear.

1 implementations.

| Implementation | Language |
| --- | --- |
| [Monotonic structures](monotonic-structures/) | C++ |

Every file here was written for this repository and verifies itself; see [`patterns/`](../../patterns/) for the extracts from earlier work.

[Back to the catalogue](../../README.md)
