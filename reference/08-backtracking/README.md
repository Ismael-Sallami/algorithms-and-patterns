# Backtracking

Depth-first search over partial solutions: extend, recurse, undo. The undo is what makes one shared buffer enough, and pruning early is what separates it from brute force.

1 implementations.

| Implementation | Language |
| --- | --- |
| [Backtracking](backtracking/) | C++ |

Every file here was written for this repository and verifies itself; see [`patterns/`](../../patterns/) for the extracts from earlier work.

[Back to the catalogue](../../README.md)
