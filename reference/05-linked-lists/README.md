# Linked lists

Pointer surgery written out rather than hidden behind a container, cycle detection in constant space, and the hash-map-plus-list pairing that makes an LRU cache constant time.

2 implementations.

| Implementation | Language |
| --- | --- |
| [List surgery and cycles](list-surgery-and-cycles/) | C++ |
| [LRU cache](lru-cache/) | C++ |

Every file here was written for this repository and verifies itself; see [`patterns/`](../../patterns/) for the extracts from earlier work.

[Back to the catalogue](../../README.md)
