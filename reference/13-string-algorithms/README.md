# String algorithms

Avoiding the restart that makes naive matching quadratic, by precomputing what can be reused. Rolling hashes and palindrome radii apply the same reuse idea differently.

2 implementations.

| Implementation | Language |
| --- | --- |
| [KMP and the Z-algorithm](kmp-and-z-algorithm/) | C++ |
| [Rolling hash and palindromes](rolling-hash-and-palindromes/) | C++ |

Every file here was written for this repository and verifies itself; see [`patterns/`](../../patterns/) for the extracts from earlier work.

[Back to the catalogue](../../README.md)
