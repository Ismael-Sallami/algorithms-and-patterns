# Two pointers and sliding window

Replacing a nested loop with two indices that only ever move forwards. The recurring question is which pointer it is safe to advance, and what property of the data makes that safe.

5 implementations.

| Implementation | Language |
| --- | --- |
| [Container with most water](container-with-most-water/) | C++ |
| [Cyclic sort and missing values](cyclic-sort-and-missing-values/) | C++ |
| [Sliding window variable size](sliding-window-variable-size/) | C++ |
| [Three way partition](three-way-partition/) | C++ |
| [Two pointer pair sum](two-pointer-pair-sum/) | C++ |

Every file here was written for this repository and verifies itself; see [`patterns/`](../../patterns/) for the extracts from earlier work.

[Back to the catalogue](../../README.md)
